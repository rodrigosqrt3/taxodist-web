---
title: "Introduction to taxodist"
format:
  html:
    include-in-header:
      text: |
        <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Mono:wght@400;500&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
        <meta name="description" content="Getting started with taxodist: compute taxonomic distances and retrieve phylogenetic lineages in R and Python.">
---

```{=html}
<style>
  :root {
    --bg:      #faf6ee;
    --border:  #5c3d1e;
    --branch:  #8b5e3c;
    --tip:     #b8865a;
    --ink:     #3d2510;
    --muted:   #6b4c30;
    --rule:    #d9c9b0;
    --card-bg: #f3ede0;
    --code-bg: #ede5d0;
  }

  body { font-family: 'DM Sans', sans-serif; background: var(--bg); color: var(--ink); }

  /* Nav */
  .site-nav {
    display: flex; align-items: center; gap: 2rem;
    padding: 1rem 0; border-bottom: 1px solid var(--rule); margin-bottom: 3rem;
  }
  .nav-brand {
    font-family: 'DM Serif Display', serif; font-size: 1.15rem;
    color: var(--ink); text-decoration: none; font-weight: 400; flex-shrink: 0;
  }
  .nav-brand:hover { color: var(--border); text-decoration: none; }
  .nav-links { display: flex; gap: 1.5rem; align-items: center; flex-wrap: wrap; }
  .nav-links a { font-size: 0.85rem; font-weight: 500; color: var(--muted); text-decoration: none; transition: color 0.15s; }
  .nav-links a:hover { color: var(--ink); text-decoration: none; }
  .nav-github { margin-left: auto; color: var(--border) !important; }
  .nav-active { color: var(--ink) !important; border-bottom: 1px solid var(--ink); }
  .nav-toggle { display: none; background: none; border: none; font-size: 1.2rem; cursor: pointer; color: var(--ink); }
  @media (max-width: 600px) {
    .nav-toggle { display: block; margin-left: auto; }
    .nav-links { display: none; width: 100%; flex-direction: column; align-items: flex-start; gap: 0.75rem; padding: 1rem 0; }
    .nav-links.open { display: flex; }
    .nav-github { margin-left: 0 !important; }
  }

  /* Page header */
  .page-header {
    padding: 3rem 0 2.5rem;
    border-bottom: 1px solid var(--rule);
    margin-bottom: 3rem;
    max-width: 680px;
  }
  .page-header .eyebrow {
    font-size: 0.72rem; font-weight: 500; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--branch); margin-bottom: 0.5rem;
  }
  .page-header h1 {
    font-family: 'DM Serif Display', serif;
    font-size: clamp(1.8rem, 3vw, 2.5rem);
    color: var(--ink); margin: 0 0 0.75rem; font-weight: 400;
  }
  .page-header p { font-size: 1rem; color: var(--muted); margin: 0; line-height: 1.7; font-weight: 300; }

  /* Prose */
  .prose { max-width: 720px; }
  .prose h2 {
    font-family: 'DM Serif Display', serif; font-size: 1.5rem;
    color: var(--ink); font-weight: 400; margin: 3rem 0 1rem;
    padding-bottom: 0.5rem; border-bottom: 1px solid var(--rule);
  }
  .prose h3 {
    font-family: 'DM Sans', sans-serif; font-size: 1rem;
    font-weight: 600; color: var(--ink); margin: 2rem 0 0.75rem;
  }
  .prose p { color: var(--muted); line-height: 1.75; margin: 0 0 1rem; font-size: 0.95rem; }
  .prose code {
    font-family: 'DM Mono', monospace; font-size: 0.82rem;
    background: var(--code-bg); color: var(--border);
    padding: 0.15em 0.4em; border-radius: 2px;
  }
  .prose a { color: var(--border); }
  .prose a:hover { color: var(--ink); }

  /* Callout box */
  .callout {
    background: var(--card-bg); border: 1px solid var(--rule);
    border-left: 3px solid var(--branch); border-radius: 4px;
    padding: 1.25rem 1.5rem; margin: 1.5rem 0;
  }
  .callout p { margin: 0; font-size: 0.88rem; color: var(--muted); line-height: 1.65; }
  .callout strong { color: var(--ink); }

  /* Formula */
  .formula-block {
    background: var(--card-bg); border: 1px solid var(--rule);
    border-left: 3px solid var(--branch); border-radius: 4px;
    padding: 1.5rem 2rem; margin: 1.5rem 0;
    display: flex; align-items: center; gap: 2rem; flex-wrap: wrap;
  }
  .formula-block p { margin: 0; color: var(--muted); font-size: 0.88rem; line-height: 1.65; flex: 1; min-width: 200px; }
  .formula-math {
    font-family: 'DM Mono', monospace; font-size: 1.1rem;
    color: var(--border); background: var(--code-bg);
    padding: 0.6rem 1rem; border-radius: 3px; white-space: nowrap;
  }

  /* Section nav (bottom) */
  .article-nav {
    display: flex; justify-content: flex-end; margin-top: 4rem;
    padding-top: 2rem; border-top: 1px solid var(--rule);
  }
  .article-nav a {
    font-size: 0.85rem; font-weight: 500; color: var(--border);
    text-decoration: none; display: flex; align-items: center; gap: 0.4rem;
  }
  .article-nav a:hover { color: var(--ink); text-decoration: none; }
</style>

<nav class="site-nav">
  <a href="../index.html" class="nav-brand">taxodist</a>
  <div class="nav-links">
    <a href="getting-started.html" class="nav-active">Get Started</a>
    <a href="statistical-applications.html">Statistical Applications</a>
    <a href="../reference.html">Reference</a>
    <a href="https://github.com/rodrigosqrt3/taxodist" class="nav-github">GitHub ↗</a>
  </div>
  <button class="nav-toggle" onclick="document.querySelector('.nav-links').classList.toggle('open')">☰</button>
</nav>

<div class="page-header">
  <div class="eyebrow">Article</div>
  <h1>Introduction to taxodist</h1>
  <p>Learn how to compute taxonomic distances and retrieve phylogenetic lineages for any pair of taxa, from dinosaurs to bacteria.</p>
</div>

<div class="prose">
```

## What is taxodist?

`taxodist` answers a simple question: *how related are any two living things?*

Given any two taxon names — a pair of dinosaurs, a dinosaur and a fungus, two
species of fly, or an oak tree and a human — `taxodist` retrieves their full
hierarchical lineages from [The Taxonomicon](http://taxonomicon.taxonomy.nl) and
computes a dissimilarity index between them.

The Taxonomicon is based on *Systema Naturae 2000* (Brands, 1989 onwards) and
provides exceptionally deep lineage resolution, substantially exceeding other programmatic sources.

Searches work at any taxonomic level: genus, species, family, order, or
any clade. Both `"Tyrannosaurus"` and `"Tyrannosaurus rex"` are valid inputs, as
are `"Drosophila melanogaster"`, `"Homo sapiens"`, or `"Araucaria angustifolia"`.

---

## The distance metric

`taxodist` measures relatedness by asking a single question:
*how deep is their most recent common ancestor?*

```{=html}
<div class="formula-block">
  <p>The deeper the shared ancestor, the smaller the distance — meaning the more related the two taxa are. A shallow MRCA (close to the root) means the two taxa diverged early; a deep MRCA means they share a long common history.</p>
  <div class="formula-math">d(A,B) = 1 / depth(MRCA)</div>
</div>
```

This has a key property: taxa that diverged at the same point
in the tree are always equidistant from any third taxon, regardless of how
many nodes each has in its lineage below the split. For example:

- *Tyrannosaurus* and *Velociraptor* are both Tetanurae: they diverged from
  *Carnotaurus* (Ceratosauria) at the same node (Averostra), so both have
  exactly the same distance to Carnotaurus.

- All dinosaurs diverged from the mammal lineage at the same
  node (Amniota), so *Homo sapiens* is equally distant from *Tyrannosaurus*, *Triceratops*, *Carnotaurus*, and *Cyanocorax*.

The distance is bounded to $[0, 1]$, where 0 means identical taxa, 1 means the taxa share only the root as their most recent common ancestor, and smaller values reflect increasingly recent shared ancestry.

---

## Installation

::: {.panel-tabset}

### R

```r
install.packages("taxodist")
library(taxodist)
```

### Python

```python
pip install taxodist
import taxodist as td
```

:::

---

## Basic usage

### Getting a lineage

```r
lin <- get_lineage("Tyrannosaurus")
tail(lin, 8)
#> [1] "Avetheropoda"     "Coelurosauria"    "Tyrannoraptora"   "Tyrannosauroidea"
#> [5] "Tyrannosauridae"  "Tyrannosaurinae"  "Tyrannosaurini"   "Tyrannosaurus"
```

Species-level searches also work:

```r
lin <- get_lineage("Drosophila melanogaster")
tail(lin, 4)
#> [1] "Ephydroidea"             "Drosophilidae"           "Drosophilinae"
#> [4] "Drosophila melanogaster"
```

### Computing distance between two taxa

```r
result <- taxo_distance("Tyrannosaurus", "Velociraptor")
print(result)
#> -- Taxonomic Distance --
#>
#> * Tyrannosaurus vs Velociraptor
#>   Distance : 0.0153846153846154
#>   MRCA : Tyrannoraptora (depth 65)
#>   Depth A : 70
#>   Depth B : 73
```

The distance grows as taxa become more distantly related:

```r
taxo_distance("Tyrannosaurus", "Homo")$distance        # 0.02777778
taxo_distance("Tyrannosaurus", "Drosophila")$distance  # 0.06666667
taxo_distance("Tyrannosaurus", "Quercus")$distance     # 0.25
taxo_distance("Escherichia", "Homo")$distance          # 1
```

### Finding the most recent common ancestor

```r
mrca("Tyrannosaurus", "Velociraptor")  # "Tyrannoraptora"
mrca("Tyrannosaurus", "Triceratops")   # "Dinosauria"
mrca("Tyrannosaurus", "Homo")          # "Amniota"
mrca("Tyrannosaurus", "Drosophila")    # "Nephrozoa"
mrca("Tyrannosaurus", "Quercus")       # "discaria"
```

---

## Working with multiple taxa

### Pairwise distance matrix

```r
taxa <- c(
  "Tyrannosaurus", "Carnotaurus", "Triceratops",
  "Parasaurolophus", "Stegosaurus", "Brachiosaurus",
  "Homo sapiens", "Homo neanderthalensis", "Pan troglodytes",
  "Panthera leo", "Canis lupus",
  "Ornithorhynchus anatinus",
  "Loxodonta africana",
  "Struthio camelus",
  "Aptenodytes forsteri",
  "Ara ararauna",
  "Crocodylus niloticus",
  "Chelonia mydas",
  "Ambystoma mexicanum",
  "Octopus vulgaris",
  "Carcharodon carcharias",
  "Balaenoptera musculus",
  "Drosophila melanogaster",
  "Apis mellifera",
  "Arabidopsis thaliana",
  "Quercus robur",
  "Ginkgo biloba",
  "Welwitschia mirabilis",
  "Saccharomyces cerevisiae",
  "Escherichia coli",
  "Bacillus subtilis",
  "Plasmodium falciparum"
)
mat <- distance_matrix(taxa)
```

The matrix is symmetric with zeros on the diagonal.

### Hierarchical clustering

```r
cl <- taxo_cluster(taxa)
plot(cl)
```

### PCoA ordination

```r
ord <- taxo_ordinate(taxa)
summary(ord)
plot(ord)
```

### Heatmap

```r
taxo_heatmap(taxa)
```

### Finding the closest relative

```r
closest_relative(
  "Carnotaurus",
  c("Aucasaurus", "Velociraptor", "Triceratops",
    "Brachiosaurus", "Homo sapiens", "Apis mellifera")
)
#>            taxon   distance
#> 1     Aucasaurus 0.01515152
#> 2   Velociraptor 0.01666667
#> 4  Brachiosaurus 0.01754386
#> 3    Triceratops 0.01818182
#> 5   Homo sapiens 0.02777778
#> 6 Apis mellifera 0.06666667
```

---

## Lineage utilities

### Comparing lineages side by side

```r
compare_lineages("Carnotaurus", "Tyrannosaurus")
#> -- Lineage Comparison --
#> MRCA: Averostra at depth 60
#>
#> Shared lineage (60 nodes):
#>   Biota ... Theropoda
#>
#> Carnotaurus only (7 nodes):
#>   Ceratosauria ... Carnotaurus
#>
#> Tyrannosaurus only (10 nodes):
#>   Tetanurae ... Tyrannosaurus
```

### Testing and filtering by clade

```r
is_member("Tyrannosaurus", "Theropoda")          # TRUE
is_member("Triceratops", "Theropoda")            # FALSE
is_member("Homo sapiens", "Amniota")             # TRUE

filter_clade(taxa, "Dinosauria")
#> [1] "Tyrannosaurus" "Carnotaurus" "Triceratops" "Velociraptor"

filter_clade(taxa, "Theropoda")
#> [1] "Tyrannosaurus" "Carnotaurus" "Velociraptor"
```

---

## Coverage and caching

### Checking coverage before a large run

```r
taxa <- c("Tyrannosaurus", "Velociraptor", "Apis mellifera", "Fakeosaurus")
check_coverage(taxa)
#>  Tyrannosaurus  Velociraptor  Apis mellifera    Fakeosaurus
#>           TRUE          TRUE            TRUE          FALSE
```

Use `check_coverage()` to pre-screen a list before `distance_matrix()` — taxa
returning `FALSE` will produce `NA` distances.

### Persisting the cache across sessions

```r
save_cache("my_taxa_cache.rds")   # end of session
load_cache("my_taxa_cache.rds")   # start of next session, before any calls
```

```{=html}
<div class="callout">
  <p><strong>Tip:</strong> Save the cache before running <code>distance_matrix()</code> on a large dataset. Lineages are cached in memory after first retrieval, so reloading a saved cache avoids all redundant network requests.</p>
</div>
```

---

## A note on lineage depth

The Taxonomicon provides substantially deeper lineage resolution than most
other programmatic sources. *Tyrannosaurus* has 70 nodes in its lineage,
capturing intermediate clades at the level of superfamilies, tribes, and
named subclades absent from most sources. This depth is what makes the
distance metric meaningful — shallower sources produce coarser distances
that conflate distantly related groups.

---

## Data source and citation

All lineage data is sourced from The Taxonomicon (taxonomy.nl), based on
*Systema Naturae 2000*:

> Brands, S.J. (1989 onwards). *Systema Naturae 2000*. Amsterdam,
> The Netherlands. Retrieved from The Taxonomicon,
> http://taxonomicon.taxonomy.nl.

Please cite this resource in any published work using `taxodist`.

```{=html}
</div>

<div class="article-nav">
  <a href="statistical-applications.html">Statistical Applications →</a>
</div>
```
